apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-deployment
  namespace: quizstar-portal
  labels:
    deployment: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      pod: mysql
  template:
    metadata:
      labels:
        pod: mysql
    spec:
      restartPolicy: Always
      containers:
      - name: mysql-container
        image: mysql:latest
        ports:
        - containerPort: 3306
        envFrom:
        - configMapRef:
            name: quizstar-portal-configmap
        - secretRef:
            name: quizstar-portal-secrets
        volumeMounts:
        - name: mysql-volume
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-volume
        persistentVolumeClaim:
          claimName: mysql-pvc